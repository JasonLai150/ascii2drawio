1) box(es) contained in larger boxes  -- DONE: nested-box detection (parent links, container emit, sibling edges); find_rectangles 3-pass + border-aware _node_at. Cross-wall edges (line crossing a container wall via ┼) now kept as one edge (_cross_wall / _passes_through).
2) multiple arrows originating from one box  -- DONE (already worked): _build_edges source/sink pairing handles fan-out + arrows on all 4 sides + labeled branches. Verified, regression tests added (examples/multi-arrow/).
3) no box outlines (just text as nodes)  -- DONE: loose mode (--loose / convert(loose=True)); find_text_nodes in nodes.py
